# Routes
# This file defines all application routes (Higher priority routes first)
# https://www.playframework.com/documentation/latest/ScalaRouting
# ~~~~

# An example controller showing a sample home page
GET           /                       controllers.HomeController.index

# Map static resources from the /public folder to the /assets URL path
GET           /assets/*file           controllers.Assets.versioned(path="/public", file: Asset)

# Mapping to /tweets
GET           /tweets                 controllers.TweetController.list

# Mapping to /tweets/:tweetId
GET           /tweets/:tweetId        controllers.TweetController.find(tweetId: Long)

# Mapping to /tweets
POST          /tweets                 controllers.TweetController.create

# Mapping to /tweets/:tweetId
PUT           /tweet/:tweetId         controllers.TweetController.update(tweetId: Long)

# Mapping to /tweets/:tweetId
DELETE        /tweet/:tweetId         controllers.TweetController.delete(tweetId: Long)
